class Solution:
    def isPowerOfFour(self, n: int) -> bool:
        # Check if n is greater than 0, is a power of 2, and its only set bit is at an even position
        return n > 0 and (n & (n - 1)) == 0 and (n - 1) % 3 == 0
