class Solution:
    def areaOfMaxDiagonal(self, dimensions: list[list[int]]) -> int:
        # Find the rectangle with the maximum diagonal length; if tied, use max area
        a, b = max(dimensions, key=lambda x: (x[0]**2 + x[1]**2, x[0] * x[1]))
        return a * b
